# Configuration for IDK-AI Application
# This file controls all service URLs and ports for easy deployment

[orchestrator]
host = 127.0.0.1
port = 8000
# Cloud Run endpoint for deployed orchestrator (used when running in cloud mode)
service_url = https://idkai-backend-454838348123.asia-south1.run.app

[module1]
host = 127.0.0.1
port = 8001
# Optional: Cloud Run endpoint for deployed Module 1
service_url =

[module2]
host = 127.0.0.1
port = 8002
# Optional: Cloud Run endpoint for deployed Module 2
service_url =

[module3]
host = 127.0.0.1
port = 8003
# Optional: Cloud Run endpoint for deployed Module 3
service_url =

[module4]
host = 127.0.0.1
port = 8004
# Optional: Cloud Run endpoint for deployed Module 4
service_url =

[frontend]
host = localhost
port = 3000
# Cloud Run endpoint for deployed frontend (used when running in cloud mode)
service_url = https://idkai-frontend-454838348123.asia-south1.run.app

[database]
# Local development DSN. Production deployments should override via DATABASE_URL env var.
url = postgresql://idkai_local:password@localhost:5432/idkai_local
echo = false

# For production deployment, change these to your server IPs/domains
# Example:
# [orchestrator]
# host = api.yourdomain.com
# port = 443
